<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape
            android:shape="oval" android:useLevel="true">
            <!-- 填充的颜色 -->
            <solid android:color="@color/colorButtonPhoneBackground" />
            <size
                android:width="56dp"
                android:height="56dp"
                />
        </shape>
    </item>

    <item
        android:drawable="@drawable/ic_action_call"
        android:height="40dp"
        android:width="40dp"
        android:gravity="center">
    </item>
</layer-list >